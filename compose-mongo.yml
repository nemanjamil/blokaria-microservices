version: '3.1'

services:
  mongo:
    image: mongo
    restart: always
    ports:
      - "27017:27017" # Exposes the default MongoDB port to localhost
    environment:
      MONGO_INITDB_ROOT_USERNAME: xaviomar
      MONGO_INITDB_ROOT_PASSWORD: xaviomar
